# API Base URL
# For local development with Docker/nginx: /api (uses nginx reverse proxy)
# For local development standalone: http://localhost:8000
# For production (Railway): Leave as /api if using nginx, or set your Railway URL
VITE_API_BASE_URL=/api
